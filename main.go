package main

import "fmt"

func main() {
    var senderName = "Bren"
    var recipient = "Adrian"
    var message = "The hole, Adrian. You have to boof the hole!"

    fmt.Printf("%s to %s: %s/n", senderName, recipient, message)

}